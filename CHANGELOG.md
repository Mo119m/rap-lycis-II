# 改进日志 (Changelog)

## 2026-02-15: Pipeline 重构与数据清洗

### 一、项目结构重构

**问题**：所有逻辑都挤在一个 `rap_ner_pipeline.py` (204 行) 里，不好维护、不好调试。

**改动**：拆成 4 个独立模块 + 1 个 CLI 入口：

| 文件 | 职责 | 行数 |
|------|------|------|
| `src/data_cleaning.py` | 数据加载、歌曲过滤、文本清洗 | ~190 |
| `src/ner.py` | spaCy NLP 构建、NER 实体提取 | ~100 |
| `src/clustering.py` | Bag-of-Entities 矩阵、K-Means、聚类摘要 | ~75 |
| `src/io_utils.py` | 输出保存（带文件大小报告） | ~35 |
| `src/rap_ner_pipeline.py` | CLI 入口（调用以上模块） | ~85 |

每个模块可以在 notebook 或脚本中独立 import 使用。

---

### 二、数据清洗新增

#### 2.1 歌曲级过滤（按 song_title）

过滤掉不属于分析范围的歌曲类型：

| 类型 | 正则匹配 | 影响 |
|------|---------|------|
| Live 版本 | `(Live)` / `（Live）` / `(live)` | ~294 首歌, ~1533 行 |
| 伴奏/Instrumental | `(伴奏)` / `伴奏$` / `(Instrumental)` | ~7 首歌, ~21 行 |

#### 2.2 文本行级清洗

从歌词文本中逐行移除非歌词内容：

| 类型 | 匹配规则 | 示例 |
|------|---------|------|
| 制作信息 | 行首匹配 `出品` / `Prod.` / `混音` / `母带` / `编曲` / `作词` / `作曲` 等 | `出品公司 AFSC MUSIC` |
| 录音信息 | 行首匹配 `录音` / `Recording` / `Mixing` / `Mastering` | `录音室: XXX Studio` |
| 结构标记 | 独立的 Verse/Hook/Chorus/Bridge 等标签行 | `[Verse 1]` / `Hook:` |
| 说话人标签 | 行首 `艺人名：` 格式（保留冒号后的歌词内容） | `小老虎：别信这一套` → `别信这一套` |

> **注意**：结构标记只移除独立标签行，不会删除歌词中提到这些词的行（如 "我的verse比你强"）。

#### 2.3 修复效果（5000 行样本测试）

```
原始数据:     5000 行, 1311 首歌, 42 位艺人
歌曲过滤后:   4784 行, 1274 首歌 (移除 37 首 Live/伴奏)
文本清洗后:   4750 行 (34 行清洗后变空被丢弃)
```

---

### 三、Bug 修复

#### 3.1 说话人标签导致实体虚高

**问题**：合作歌曲中 `小老虎：别信这一套` 的 "小老虎" 被 NER 识别为 ARTIST 实体，导致周士爵的歌词里 "小老虎" 出现了 275 次（实际是说话轮标签，不是歌词内容）。

**修复**：在文本清洗阶段用正则 `^[\w\s.\-]{1,20}[：:]\s*` 剥离行首的说话人标签。

**效果**：
- "小老虎" 实体：353 → 72 次
- ARTIST 类型总量：608 → 263 次

#### 3.2 K-Means 聚类被异常值主导

**问题**：直接用 raw entity count 做 K-Means，歌词量大的艺人（如 AR 有 404 个实体）成为 outlier，聚类极度不平衡：34/1/1/6。

**修复**：在 K-Means 之前对矩阵做 L2 归一化，消除歌词量的影响。

**效果**：
```
修复前: Cluster sizes = 34 / 1 / 1 / 6  (周士爵和 AR 各占一个 cluster)
修复后: Cluster sizes = 21 / 12 / 4 / 5 (合理且可解释)
```

聚类结果变得有意义：
- Cluster 0: 地下/real hip-hop 风格
- Cluster 1: 北京说唱场景（周士爵/小老虎/龙井说唱）
- Cluster 2: 街头/东北风格
- Cluster 3: 长沙说唱场景（C-Block/刘聪/功夫胖）

---

### 四、词典扩充

`configs/rap_lexicon_seed.jsonl`: 13 → 84 条

| 类型 | 数量 | 新增示例 |
|------|------|---------|
| ARTIST | 4 → 27 | 马思唯, Jony J, 法老, 刘聪, 杨和苏 等 |
| CITY | 4 → 18 | 广州, 深圳, 武汉, 西安, 长沙, 昆明 等 |
| BRAND | 2 → 12 | Prada, Supreme, Bape, Rolex, 奔驰, 宝马 等 |
| CULTURE | 2 → 15 | freestyle, battle, cypher, diss, flow, trap 等 |
| PLATFORM | 1 → 4 | 中国新说唱, 说唱新世代, 说唱听我的 |
| CREW (新增) | 0 → 7 | CDC, GOSH, 活死人, 红花会 等 |

---

### 五、新增 main.ipynb

一个可以直接 Run All 的主 notebook，包含：

1. **数据清洗报告** — 移除了多少行、多少首歌
2. **NER 实体提取** — Top 30 实体、各标签分布、各标签 Top-5
3. **聚类分析** — 各聚类艺人列表、代表实体
4. **质量检查** — 单艺人详细核验、可疑实体检测（singleton 实体、过长实体）

---

### 六、全量数据清洁度审计（25156 行）

对清洗后的全量数据做了系统审计，结论如下。

#### 已解决 ✅

| 问题 | 状态 |
|------|------|
| Live 版本 / 伴奏歌曲 | ✅ 已过滤（移除 301 首歌, 1554 行） |
| 行首制作信息（出品/Prod./混音/母带/编曲/作词/录音等） | ✅ 全部清除，残留 0 行 |
| 说话人标签（小老虎：、周士爵：等） | ✅ 已剥离（减少 ~400 个虚假实体） |
| K-Means 聚类不平衡 | ✅ L2 归一化后聚类合理 |

#### 已修复（本次新增）✅

| 问题 | 影响 | 处理 |
|------|------|------|
| 版权声明文本 | "未经著作权人书面许可…" 出现在 21+ 艺人, 68+ 首歌 | ✅ 新增过滤规则 |
| 同一艺人文本去重 | 同 artist 下完全相同的 text chunk 被重复计数（2971 行） | ✅ 在 combine_by_artist 前去重 |

#### 不需要处理 ✓（确认为合理数据）

| 内容 | 数量 | 判断理由 |
|------|------|---------|
| 录音/录像/视频 在歌词正文中 | 238/11/131 chunks | ✓ 是歌词内容（"在录音室回到飞行器上了"、"拍个录像 回去传给你学习"），不是 metadata |
| 纯英文歌词 | 2259 行 (9%) | ✓ 中文说唱的 code-switching 是真实现象（王嘉尔 79%、Higher Brothers 34%），属于分析范围 |
| Remix 歌曲 | 125 首, 540 行 | ✓ Remix 有独立歌词，属于分析范围 |
| 极短文本 (<20字符) | 1767 行 (7%) | ✓ 大部分是合法短歌词行，对 NER 影响小（短行提取不到多少实体） |

#### 需要注意但暂不处理 ⚠️

| 问题 | 影响 | 原因 |
|------|------|------|
| Cypher/合作歌曲跨艺人重复 | 同一段歌词出现在多个艺人下（~6000 行） | 数据源的结构问题：如"成都集团2020cypher"每位参与者各有一份完整 cypher 文本。如果目标是分析单个艺人的用词，需要人工拆分 verse。当前分析以"全文归属"为准，聚类时 L2 归一化已缓解影响 |
| Rap 式重复词 | "Bape Bape Bape Bape Bape" 一首歌贡献 123 次 | 是真实歌词（rap 中的故意重复），L2 归一化已缓解。如需更严格控制，可后续加 per-song entity cap |
| 没有 zh_core_web_lg | 目前只靠词典 (84 patterns) 做 NER | 安装 `python -m spacy download zh_core_web_lg` 后实体数量和类型会大幅增加 |
| 说话人标签正则可能过宽 | `^[\w\s.\-]{1,20}[：:]` 可能误删 "爱情：是一种毒药" 这类行 | 目前观测影响极小（抽样 5000 行只有 2 行匹配），持续关注 |

---

### 七、结论：数据是否 clean enough？

**对于当前项目目标（NER → bag-of-entities → 聚类）：是的，够用了。**

清洗后的数据质量：
- 25156 → ~22000 行（去重后）有效歌词
- 241 位艺人, 7391 首歌
- 制作 metadata 已全部清除
- Live/伴奏已过滤
- 说话人标签已剥离
- 版权声明已移除
- 聚类已归一化，不受歌词量差异影响

主要风险点是 **cypher 合作曲的跨艺人重复**（~6000 行），但这在当前聚类分析中因为 L2 归一化，影响已被控制。如果后续要做更精确的 per-artist 分析，需要人工拆分合作歌曲的 verse 归属。
